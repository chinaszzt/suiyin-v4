# MkDocs build output
/site/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# macOS
.DS_Store

# suiyin-flow 运行时产物 (verify_report.json / session logs / worktrees/)
.suiyin/
/worktrees/

# v4 self-install dogfood 产物 — v4 仓内跑 bin/init.sh 会生成下面这些
# 业务项目运行时副本，v4 source of truth 在 skills/ / runtime/，不该 commit
# 锚定 repo root（leading slash），避免误吞 dogfood/T-NNN/ 等子目录下的 evidence 快照
/.specify/
/.claude/skills/
/.claude/settings.suiyin-suggested.json
/README.suiyin-suggested.md
